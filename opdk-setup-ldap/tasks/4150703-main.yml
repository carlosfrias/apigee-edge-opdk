---
- name: Setup LDAP
  shell: "{{ apigee_setup }} -f {{ opdk_installation_config_file }} -p ld"
  register: ldap_results
  when: ld_port_status | failed

- name: Ldap results report
  debug:
    var: ldap_results
