---
- hosts: localhost
  connection: local
  vars:
    system_config_files:
    - { dir: '~', name: '.bashrc' }
    - { dir: '~', name: 'id_rsa.pub' }
    - { dir: '~', name: '.profile' }

  roles:
  - opdk-setup-system-configs-logs