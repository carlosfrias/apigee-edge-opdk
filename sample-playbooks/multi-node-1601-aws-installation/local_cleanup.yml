---
- hosts: localhost
  connection: local

  tasks:
  - name: Remove .ansible_cache
    file:
      path: '{{ item }}'
      state: absent
    with_items:
    - '.ansible_cache'
    - 'ansible.log'
    - 'ssh_keys'
    - 'config_and_logs'
