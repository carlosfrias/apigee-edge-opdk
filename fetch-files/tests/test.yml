---
- hosts: localhost
  connection: local
  vars:
    fetch_files:
    - { dir: '~', name: '.bashrc' }
    - { dir: '~', name: 'id_rsa.pub' }
    - { dir: '~', name: '.profile' }

  roles:
  - fetch-found-files
